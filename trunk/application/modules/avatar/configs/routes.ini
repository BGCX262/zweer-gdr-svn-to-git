[production]

routes.avatar.route = avatar/:controller/:pg
routes.avatar.defaults.action = index
routes.avatar.defaults.module = avatar

routes.blogPermalink.route = avatar/diario/:pg/:pagina
routes.blogPermalink.defaults.action = permalink
routes.blogPermalink.defaults.controller = diario
routes.blogPermalink.defaults.module = avatar